# Trivy Ignore File
# Ignore vulnerabilities in base Node.js image's npm installation
# These are system dependencies we cannot control

# glob vulnerability in Node.js base image npm
CVE-2025-64756

# tar vulnerabilities in Node.js base image npm
CVE-2026-23745
CVE-2026-23950
CVE-2026-24842

# NOTE: We've added npm overrides in package.json for glob and tar
# to ensure our application dependencies use secure versions.
# These ignores only apply to the system npm in the base image.
